name: Deploy API Explorer to Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      # Both explorers are fully self-contained; the spec corpora ride along so
      # each detail pane's "Source <file>" link resolves on the public site too.
      - run: |
          mkdir -p dist
          node src/build-explorer.js --out dist/index.html --xlink graphql.html
          node src/build-graphql-explorer.js --out dist/graphql.html --xlink ./
          cp -R openapi dist/openapi
          cp -R graphql dist/graphql
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist
      - id: deployment
        uses: actions/deploy-pages@v4
