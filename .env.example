# ServiceNow instance connection — copy to .env and fill in.
# A bare name ("dev12345") or host is normalized to https://<name>.service-now.com
SN_INSTANCE_URL=dev12345
SN_USERNAME=admin
SN_PASSWORD=

# Optional: server port for src/server.js (default 3000)
PORT=3000
